import React from 'react';
import ReactDOM from 'react-dom';
import Color from './Color';
import './index.scss';

const rootElem = document.getElementById('root');

ReactDOM.render(<Color />, rootElem);